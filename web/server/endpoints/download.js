const {
  ArrayType,
  Arguments,
  Diagnostic,
  UserArguments,
  namedFile,
  Enum2,
  Enum1,
  Enum0
} = require("../types.js");

const {
  Job,
  CompiledFile
} = require("../models.js");

/**
 * res_output
 */

/**
 * res_input
 * @param file_id: String
 */
module.exports = async function process_download(res_input, res_output) {
  // TODO: implement
  return res_output;
}