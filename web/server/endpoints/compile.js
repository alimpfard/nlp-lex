const {ArrayType, Arguments, Diagnostic, Enum1, Enum0} = require("../types.js");

const {Job} = require("../models.js");

/**
 * res_output
 * @param b64encoded: String
 */

/**
 * res_input
 * @param job_id: String
 */

let constructB64encoded = x => (String).call(null, x);


module.exports = async function process_compile(res_input, res_output) {
	// TODO: implement
	return res_output;
}