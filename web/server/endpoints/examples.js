const {
  ArrayType,
  Diagnostic,
  Arguments,
  Enum2,
  Enum1,
  Enum0
} = require("../types.js");

const {
  Job
} = require("../models.js");

/**
 * res_output
 * = ArrayType(String)
 */

/**
 * res_input
 */
module.exports = async function process_examples(res_input, res_output) {
  return Enum2.choices;
}